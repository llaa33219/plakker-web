{
  "name": "plakker",
  "version": "1.0.0",
  "description": "이모티콘 공유 사이트",
  "scripts": {
    "dev": "ENVIRONMENT=development wrangler pages dev .",
    "dev:nocache": "ENVIRONMENT=development wrangler pages dev . --compatibility-date=2024-01-01 --local",
    "deploy": "wrangler pages deploy .",
    "publish": "wrangler pages deploy ."
  },

  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}